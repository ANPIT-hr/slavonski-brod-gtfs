# Built feed bundle — regenerated for each release and attached to GitHub Release.
# Don't track it in the repo to avoid noisy diffs of binary archives.
dist/

# MobilityData GTFS Validator output
validation_report/
*.validator.log

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# Validator jar is too big to track; the helper scripts in tools/ are tracked
tools/*.jar

# Local Claude config (not shared)
.claude/settings.local.json

# Python build junk
__pycache__/
*.pyc

web/.env
